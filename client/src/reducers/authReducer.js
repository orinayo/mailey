const initialAuth = {
  token: ''
}

const authReducer = (state = initialAuth, action = {}) => {
  switch (action.type) {
    default:
      return state
  }
}

export default authReducer
